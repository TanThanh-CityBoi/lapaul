{{>header}}

<div class="container align-items-center min-vh-100">
        <div class="col-md-6 col-sm-12">
            <h1 class="text-center mt-3 mb-2 mt-5">Quên mật khẩu</h1>
            <p class="text-center text-secondary px-sm-2">Vui lòng nhập mã OTP được gửi qua email <b>{{email}}</b> để khôi phục mật khẩu.</p>
            <form method="post" action="/forgot-password-step-2" class="px-4 px-sm-4 px-m-5 px-lg-4 pt-3">
                <div class="renew">
                        <input id="email" name="email" value="{{email}}" type="hidden" class="form-input" placeholder="Nhập email">
                    <div class="form-group">
                        <input id="otp" name="otp" type="phone" class="form-input" placeholder="Mã OTP xác nhận">
                    </div> 
                   {{#if error}}
                    <div class="form-group">
                        <div class="alert alert-danger">{{error.msg}}</div>
                    </div>
                {{/if}}
                </div>
                <div class="input-group custom-control custom-checkbox pt-2">
                    <div class="custom-button text-right">
                        <a href="/login" class="btn btn-logout">Trở về</a>
                        <button type="submit" class="btn btn-login">Xác nhận</button>
                    </div>
                </div>
                <div id = 'error-activeAccount' style="display: none;" class='alert-danger'></div>
            </form>
        </div>
</div>